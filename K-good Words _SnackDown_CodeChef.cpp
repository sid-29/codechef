#include<iostream>
#include<strings.h>
using namespace std;
int main()
{
    int n,a;
    cin>>n;
    for (int i=0;i<n;i++)
    {
        string str;
        char ch;
        cin>>str>>a;
        ch1=str[0];
        ch2=str[1];
        while(ch1!='/0'||ch2!='/0')
        {
            for (i=0;i<stringlen(str);i++)
            {

                ch=str[i];
            }
        }
    }
